/** Bela "die-cut" ivica (kao nalepnica) + meka senka — isto za sve slike/balone. */
export const DIE_CUT =
  "drop-shadow(3px 0 0 #fff) drop-shadow(-3px 0 0 #fff) " +
  "drop-shadow(0 3px 0 #fff) drop-shadow(0 -3px 0 #fff) " +
  "drop-shadow(0 12px 18px rgba(70,20,35,0.25))";

/** Samo meka senka — za slike koje već imaju belu ivicu zapečenu u PNG. */
export const SOFT = "drop-shadow(0 16px 24px rgba(70,20,35,0.28))";
